namespace FanaBridge.SegmentDisplay
{
    /// <summary>Special hardware commands that bypass the text render pipeline.</summary>
    public enum DeviceCommand
    {
        /// <summary>Show the Fanatec logo (firmware command).</summary>
        FanatecLogo,
    }
}
