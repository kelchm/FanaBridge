1. [BLOCKER-1] FIXED — `ExclusiveParamsOnCatalogPage` now derives ownership only from a single catalog placement, so real PBME params 1 and 4 (placed on all five pages) survive while exclusive param 505 is deleted.
2. [BLOCKER-2] NOT-FIXED — the real MessageBox uses a fresh pre-confirm snapshot, but confirmation/counting and deletion reopen different sessions and independently select/count content, while `CanRemovePageContent` accepts any nonempty catalog without resolving the target page.
3. [BLOCKER-3] NOT-FIXED — `UpdateSummon` starts from a clone, but replacing `Condition.Source` and the entire `Lifetime` still drops source extension data plus lifetime direction, then-state, and extension data that the form does not author.
4. [MAJOR-1] NOT-FIXED — `EnsureAuthoredRow` clones its argument, but both real reorder call sites still construct and pass a sparse kind/id/target seed, so the end-to-end materialization still loses the omitted authored fields.
5. [MAJOR-2] FIXED — `SetInSessionPage` now accepts only ITM/hosted refs, clones the accepted ref, and rejects cycle/other refs without mutation while surfacing the ruled validation note.
6. [MAJOR-3] FIXED — remembered Manual seconds now live on the view, survive Poll reconstruction and uncheck, and are restored when the timer is checked again.
7. [MAJOR-4] NOT-FIXED — the 5f form adds the missing information, but still substitutes a ComboBox for the drawn three-segment source control, has no editable inline duration, and renders the until-dismissed consequence as plain text rather than the drawn bordered card.
8. [MAJOR-5] NOT-FIXED — Q6 handlers exist locally, but `DisplayTabPanel` never subscribes `PagesAndFieldsRequested`, the overflow fields item is unconditionally disabled, and layer-row clicks therefore have no real destination.
9. [MAJOR-6] FIXED — Poll now consumes `IMappedRoleCatalog` mapping state, rejects all-roles fallback as unmapped, and passes the real next/previous flags into every projection.
10. [MAJOR-7] FIXED — cycle glossary state is now one mutable flag per projection and each cycle composes its period with the selected summon’s actual lifetime.

NEW [BLOCKER] `DisplayTabPanel.xaml.cs:217` binds `DisplayPriorityV2View` without the wheel catalog, so the primary ITM view always sees `_catalog == null` and its remove-all option is permanently disabled despite the owner ruling.

NEW [MAJOR] `DisplayPriorityV2View.xaml.cs:1404` selects the SimHub-property ComboBox item for a built-in picker result; the synchronous selection-change handler overwrites `_epSourceKind`, so saving a built-in source can persist it as `simHubProperty`.

Verification: focused Priority/edit-session tests 82/82 passed; full solution tests 2128/2128 passed.

COMMIT-SAFE no
