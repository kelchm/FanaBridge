| # | Delta item | Verdict | Evidence |
|---|---|---|---|
| 1 | Non-atomic session check/apply replaced by runtime CAS | FIXED | `DeviceDisplayRuntime.TryApplyDisplayConfigV2` normalizes first and publishes with `Interlocked.CompareExchange(ref _configV2, next, expected)`; the host routes the session to it, and the runtime conflict test preserves the competing document. |
| 2 | `ChildRef` absence is null-only | FIXED | Last-summon satellite removal requires `row.ChildRef == null`; an extension-data-only/future `ChildRef` is retained by the regression test. |
| 3 | Typed `ActsAsEntrypointTarget` plus case-insensitive IDs | FIXED | The delimiter path grammar is gone; field/layer targeting is typed and override, page, and layer IDs use `StringComparison.OrdinalIgnoreCase`, with mixed-case tests. |
| 4 | Caller-owned subtrees are cloned | FIXED | `AddSummon`, `SplitSatellite(..., ChildRef)`, and `SetIdle` attach `CloneNode` results; tests mutate the caller objects afterward and the session document remains unchanged. |
| 5 | Clone failure is fail-closed and produces ruled notes | FIXED | `Clone`/`CloneNode` throw `InvalidOperationException` instead of returning defaults, mutation retains the prior document on failure, and validation-probe failure maps to `DisplayCopy.ConfigEditCloneFailed`. |
| 6 | `DisplayConfigV2ApplyResult.Message` contract | FIXED | Success carries null; conflict carries `DisplayCopy.ConfigEditConflict`; the regression asserts the literal user-facing message (not a tautology), and the runtime CAS integration asserts the typed result message. |

SUFFICIENCY: NOT SUFFICIENT — bake reads `_configV2 == null`, then an edit session opened against null can successfully CAS null→user document, after which bake's unconditional `SetConfigV2(baked)` overwrites that user document.

NEW [BLOCKER]: Make bake publish-if-absent atomically (or otherwise serialize/recheck its write); its current check-then-plain-write can still lose user edits.

Focused tests: PASS (34/34 `DisplayConfigV2EditSessionTests`); `git diff --check`: PASS.

COMMIT-SAFE no
